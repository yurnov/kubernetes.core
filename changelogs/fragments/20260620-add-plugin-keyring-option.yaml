---
minor_changes:
  - helm_plugin - add ``--keyring`` argument to allow changing the keyring default location. The option only applies to ``state=present`` (the ``helm plugin install`` subcommand), as that is the only implemented subcommand that supports ``--keyring``; it is ignored with a warning for any other state (https://github.com/ansible-collections/kubernetes.core/pull/1150).
